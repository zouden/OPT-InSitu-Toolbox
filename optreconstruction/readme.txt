1. Download and install DIPimage toolbox (http://www.diplib.org/download). Initiate the toolbox before runing the code.
2. Obtain sensor calibration data, name the variable "IcalIm" and save it in "calibrationImage.mat".
3. Run ReconstructSingleFish.m. The reconstruction will be saved as .mat files under the same directory as the video.